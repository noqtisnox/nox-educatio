export type DeadlineItem = {
  courseName: string;
  assignmentName: string;
  dueDate: string;
};

export type NewsItem = {
  title: string;
  link: string;
};

export type AnnouncementItem = {
  to: string;
  body: string;
}